function [a] = NextVertex(a, i, L, k)

    if i < L
       ai+1 = 1
       return (a, i + 1)
    else
        for j ? L to 1
            if aj < k
            aj ? aj + 1
            return (a, j)
            end
         return (a, 0)
        end
    end